# nlp-utils-playground

A collection of small, self-contained Python utilities for NLP and ML experiments —
text preprocessing, embedding helpers, evaluation snippets, and notebook
boilerplate.

Each utility lives in its own file under `utils/` and is intended to be
copy-pasteable into other projects.
